{
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks 2>/dev/null || true",
    "smoke:notify": "node scripts/notify-smoke.js",
    "smoke:digest": "node scripts/digest-smoke.js"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.2",
    "agentation": "^3.0.2",
    "firebase": "^12.13.0",
    "firebase-admin": "^13.10.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
